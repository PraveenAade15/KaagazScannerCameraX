package com.example.kaagazscanner.Room
/*
import android.provider.SyncStateContract
import androidx.room.Entity
import androidx.room.PrimaryKey
import com.example.kaagazscanner.MainActivity

@Entity(tableName = SyncStateContract.Constants.ACCOUNT_NAME)
class ResponseEntity(
    val responseModelList: MutableList<MainActivity>
) {
    @PrimaryKey(autoGenerate = false)
    var id: Int = 0
}

 */